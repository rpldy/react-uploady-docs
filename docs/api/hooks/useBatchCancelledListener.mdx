---
id: useBatchCancelledListener
title: "useBatchCancelledListener"
pagination_label: "useBatchCancelledListener"
sidebar_label: "🪝 useBatchCancelledListener"
tags: [hook, event, batch]
---

## Package

**_[@rpldy/uploady](../../../packages/rpldy-uploady)_**

## Installation

```bash npm2yarn
npm install @rpldy/uploady
```

## Details

```typescript
type useBatchCancelledListener = BatchEventHook;
```

- See: [BatchEventHook](../../types#batcheventhook)
- See: [Batch Entity](../../entities/#batch)

:::note
Event Hook - [BATCH-CANCEL](../../events#batchCancel)
:::

Called in case batch was cancelled programmatically

:::info
This event can be scoped to a specific batch by passing the batch id as a second parameter
:::

## Example

```javascript
import { useBatchCancelledListener } from "@rpldy/uploady";

const MyComponent = () => {
    useBatchCancelledListener((batch, options) => {
        console.log(`batch ${batch.id} was cancelled`);
    });

    //...
};
```
